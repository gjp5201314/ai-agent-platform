## 2026-07-11 - 前端 UI 科技风重设计

- 完成了整个前端 UI 的科技风/赛博朋克风格重设计
- 修改文件：tailwind.config.js, index.css, index.html, App.tsx, Sidebar.tsx, ChatInterface.tsx, MessageList.tsx, MessageInput.tsx, DocumentUpload.tsx, Settings.tsx

**设计变更要点：**
- 配色：从蓝色+石板灰亮色主题 → 深空暗色主题 (surface-900~400) + 青色霓虹 (cyber) + 紫色 (neon)
- 效果：玻璃拟态 (glass-panel)、霓虹辉光 (shadow-glow-*)、网格背景 (bg-cyber-grid-animated)、环境光球
- 动画：glow-pulse, scan-line, float, shimmer, flicker-soft 等科技感动画
- Markdown：深色代码块 + 青色霓虹主题的代码高亮、链接、表格
- 所有组件保持原有功能和 API 接口不变
- 构建成功，无错误

## 2026-07-11 - 添加 Agent 切换器

- 问题：用户可以创建多个 Agent，但前端没有切换入口
- 修改文件：App.tsx, Sidebar.tsx, ChatInterface.tsx
- App.tsx：新增 agents 列表状态、loadAgents、handleSwitchAgent；关闭 Settings 时自动刷新 Agent 列表
- Sidebar.tsx：在 Header 区域新增 Agent 下拉选择器，显示所有 Agent，点击切换；底部"管理 Agent"链接
- ChatInterface.tsx：接收 activeAgent prop，在移动端头部显示当前 Agent 名称
- 切换 Agent 时自动开始新对话
- 构建成功，零错误

## 2026-07-11 - 支持图片和文件上传询问 Agent

- 功能：用户可以上传图片（JPEG/PNG/GIF/WebP）让 Agent 分析，也可以上传 PDF/TXT/代码文件等让 Agent 读取内容
- **后端改动：**
  - config.py：新增 upload_dir、qwen_vision_model、文件上传限制配置
  - schemas.py：新增 AttachmentInfo schema，ChatRequest 新增 attachments 字段
  - main.py：添加 StaticFiles 挂载 /uploads 目录
  - api/chat.py：新增 POST /api/chat/upload 上传接口；_messages_to_langchain 支持多模态消息（文本+图片）；_build_user_message_content 提取文件文本内容注入消息
  - agent/nodes.py：_ensure_str_content 保留图片块不再剥离；rag_node 支持从多模态消息中提取文本进行检索
  - agent/llm.py：get_llm 新增 has_images 参数，上传图片时自动切换到 qwen-vl-plus 视觉模型
- **前端改动：**
  - types/index.ts：新增 Attachment 类型
  - api/client.ts：新增 uploadAttachment API；streamChat 新增 attachments 参数
  - useChat.ts：sendMessage 新增 attachments 参数
  - App.tsx：handleSend 传递 attachments
  - MessageInput.tsx：新增 📎 上传按钮、文件/图片预览缩略图、删除附件功能
  - MessageList.tsx：消息气泡内显示图片缩略图和文件链接
- 前端构建通过，后端语法检查通过

## 2026-07-11 - 修复线上 502 Bad Gateway

- 问题：线上访问报 502 Bad Gateway (nginx/1.31.2)
- 根因：`backend/.env` 中数据库/Redis hosts 是 `localhost`（本地开发配置），部署到 Docker 容器后 `localhost` 指向容器自身而非其他服务
- 后端容器启动时 `init_db()` 连接 `localhost:5432` 失败 → 后端崩溃 → nginx 无法代理 → 502
- 解决：在 `docker-compose.yml` 的 backend 服务添加 `environment` 块，强制覆盖 POSTGRES_HOST=postgres、REDIS_HOST=redis、APP_HOST=0.0.0.0、APP_DEBUG=false、DATABASE_URL
- Docker Compose 中 `environment` 优先级高于 `env_file`，本地 `.env` 保留 localhost 开发配置不受影响
- 同步更新 `部署踩坑记录.md` 新增问题 10

## 2026-07-11 - 本地开发环境启动成功

- Docker Desktop 已安装 (v29.6.1)，PostgreSQL+pgvector、Redis 容器运行正常
- 创建 backend/.env 本地配置（localhost 数据库连接）
- Python 依赖安装到 managed venv（C:/Users/甘俊培/.workbuddy/binaries/python/envs/default）
- 后端启动成功：uvicorn http://127.0.0.1:8000 — health/db/redis 全部 ok
- 前端启动成功：Vite http://localhost:5173 — 代理 /api 到后端
- 创建 start-dev.bat 一键启动脚本
- 本地 500 错误：后端未启动导致 Vite 代理连接失败，启动 uvicorn 后恢复

## 2026-07-11 - 修复线上 502（StaticFiles uploads 目录）

- 问题：main.py 新增的 app.mount("/uploads", StaticFiles(directory="uploads")) 在容器中目录不存在，后端崩溃
- 根因：StaticFiles 初始化在模块加载时执行（早于 lifespan 的 os.makedirs）
- 修复：mount 前先 os.makedirs；Dockerfile 中 RUN mkdir -p uploads

## 2026-07-11 - 生成面试项目解析文档

- 生成 `面试项目解析.md`，全面分析项目技术栈、选型理由、替代方案对比、架构设计
- 覆盖每个技术选型的"为什么选、为什么不选、优缺点"三维对比（FastAPI/LangGraph/pgvector/Redis/React+Vite+Tailwind）
- 整理 8 个关键踩坑记录（LangChain 版本冲突、国内网络适配、502 Bad Gateway、SSE 会话管理、Agent 无限循环防护等）
- 总结 8 大项目亮点和 7 个可改进方向
- 附面试可用的"一句话总结"模板

## 2026-07-11 - 输入框支持粘贴图片

- 修改文件：frontend/src/components/MessageInput.tsx
- 新增 `handlePaste` 处理函数，监听 textarea 的 onPaste 事件
- 从 `e.clipboardData.items` 中检测图片 MIME 类型（支持 png/jpeg/gif/webp/bmp/tiff）
- 检测到图片时 `preventDefault()` 阻止 blob URL 写入文本区
- 从 blob 创建 File 对象，生成唯一文件名 `clipboard-{timestamp}-{random}.{ext}`
- 复用现有的 `uploadFile` 流程上传到后端，自动添加为附件
- 无图片时不影响正常文本粘贴行为
- 更新底部提示文字：`📎 上传/Ctrl+V 粘贴图片`
- 提取 `uploadFile` 为共享辅助函数，供 `handleFileSelect` 和 `handlePaste` 共同使用

## 2026-07-11 - 修复图片询问报错（base64 编码）

- 问题：粘贴/上传图片后询问 AI 报错 `InternalError.Algo.InvalidParameter: The provided URL does not appear to be valid`
- 根因：`_messages_to_langchain` 拼装的多模态消息中 `image_url` 使用相对路径 `/uploads/202607/xxx.png`，DashScope 远程 API 无法访问服务器本地文件
- 修复：在 `backend/app/api/chat.py` 新增 `_image_url_to_data(url)` 函数
  - 检测 URL 是否为本地路径（`/uploads/...`）
  - 读取文件 → base64 编码 → 返回 `data:image/png;base64,...`
  - 对已经是 `data:` 或 `http(s):` 的 URL 直接透传
  - 文件不存在时回退到原始 URL
- `_messages_to_langchain` 中调用 `_image_url_to_data(att["url"])` 替代直接使用 `att["url"]`
- Python 语法检查通过
- tsc 类型检查 + vite build 均通过，零错误

## 2026-07-11 - 修复图片缩略图不显示

- 问题：图片粘贴上传后，聊天记录中的缩略图显示为占位符（alt 文字）
- 根因：
  - 缩略图 `<img src="/uploads/202607/xxx.png">` 在 dev 环境 vite 没代理 `/uploads/`
  - prod 环境前端容器的 nginx 也没代理 `/uploads/`
- 修复（双管齐下，更稳健的方案是前端 base64）：
  - **前端 MessageInput.tsx**：上传图片后用 FileReader 读成 base64 data URL 替换 `att.url`，这样：
    1. 缩略图直接渲染（无需任何代理）
    2. LLM vision API 也能直接读取（不依赖远程可访问性）
  - **vite.config.ts**：dev 代理补上 `/uploads → http://localhost:8000`
  - **frontend/nginx.conf**：prod 代理补上 `location /uploads/ → backend:8000`
- 非图片文件仍用服务端 URL（不需要 base64）
- vite build 通过

## 2026-07-11 - 接入 shadcn/ui + Tailwind v4 升级

- 初始化 shadcn/ui v4 + Tailwind CSS v4.3.2
- 配置 tsconfig.json path alias `@/* → ./src/*` 和 vite.config.ts resolve.alias
- 安装组件：Button, Dialog, Input, Textarea, Label, Slider, Switch, Select, ScrollArea, Command, Sonner, Separator
- Tailwind v3 → v4 迁移：PostCSS tailwindcss → @tailwindcss/vite 插件；`@tailwind base/components/utilities` → `@import "tailwindcss"`；`@apply` → 直接 CSS 变量
- 自定义 cyberpunk 暗色主题适配为 shadcn CSS 变量（--background, --foreground, --primary, --secondary, --muted, --accent, --border 等）
- **Settings.tsx** 重写：Dialog + Button + Input + Textarea + Slider + Switch + Label + Separator
- **DocumentUpload.tsx** 重写：Dialog + Button
- **Sidebar.tsx** 重写：Button + Input + Separator，下拉菜单适配主题色
- **MessageInput.tsx** 重写：Button + Textarea 替代手写样式
- **App.tsx**：添加 Sonner Toaster 组件
- 所有组件保留原有功能和交互逻辑，仅替换 UI 实现
- 解决 Slider onValueChange 类型兼容问题（base-ui/react slider uses `number | readonly number[]`）
- 构建成功，CSS 69KB / JS 510KB (gzip 12.5KB + 160KB)

## 2026-07-11 - 修复 PostCSS `@layer base` 报错

- 问题：浏览器打开后报 `[postcss] @layer base is used but no matching @tailwind base directive is present`，500 Internal Server Error
- 根因：Tailwind v4 不再使用 `@config "../tailwind.config.js";` 指令读取 v3 配置文件；shadcn/tailwind.css 内部使用了 v4 原生 `@theme inline`，但 v3 风格残留 + v4 配置缺失导致冲突
- 修复：
  1. 删除 `@config "../tailwind.config.js";` 指令
  2. 在 index.css 末尾添加 `@theme { ... }` 块，使用 v4 原生 CSS 变量定义 cyber/neon/surface/accent 颜色、background-image、shadow、animate
  3. 同步把 keyframes 从 `tailwind.config.js` 移到 index.css（v4 配置文件不生效）
- 重启 vite dev server：杀掉旧 PID 24796（占用 5173），重新启动后服务在 5173 正常监听
- 最终构建：CSS 74KB / JS 510KB (gzip 13KB + 160KB)

## 2026-07-11 - 强化 RAG 启用状态视觉提示

- 修改文件：frontend/src/components/MessageInput.tsx
- 三个层级叠加让 RAG 状态不可忽视：
  1. 顶部悬浮徽章：输入框上方显示带 `animate-ping` 绿点的「知识库检索已开启」pill 标签，绿色光晕
  2. 边框色变化：RAG 开启时整个输入框边框变绿色 + 淡绿色 ring
  3. 按钮变绿：RAG 按钮背景/文字变绿色 + 右上角红点 pulse 动画 + 绿色辉光阴影
- 关闭时所有视觉提示全部消失，回到中性状态
- 构建通过，CSS 77KB / JS 511KB (gzip 13.3KB + 161KB)

## 2026-07-11 - 修复 Settings / DocumentUpload 弹窗样式

- 修改文件：frontend/src/components/Settings.tsx, DocumentUpload.tsx
- 关键修复：
  1. **宽度溢出**：用 `!max-w-5xl w-[90vw]` 覆盖 shadcn 默认的 sm 宽度 (max-w-lg)，Settings 弹窗从 600px 扩到 1024px
  2. **布局错位**：把 flex 改为 `grid-cols-[220px_1fr]` 固定左侧边栏宽度，右侧 flex column
  3. **表单控件被挤压缩小**：name/description 改为 grid-cols-2 并排；温度/最大 token 改为 grid-cols-2
  4. **滑块没有 track**：用 shadcn Slider 组件（之前用的 native input），自带动效 + 圆形 thumb
  5. **添加 ScrollArea**：左侧 Agent 列表 / 右侧表单都加滚动区，长内容不会撑爆
  6. **添加 DialogDescription**：补充副标题「管理你的 AI Agent 配置...」
  7. **工具开关改为 grid-cols-2**：原来是一列 1 个，10 个工具导致表单超长
- 温度和相似度阈值 slider 加上「精确/创意」「宽松/严格」极值标签
- 工具开关加「N/M 已启用」计数
- DocumentUpload 同样加宽到 max-w-2xl、补充 description、加 ScrollArea
- 构建通过，CSS 77KB / JS 529KB (gzip 13.4KB + 166KB)

## 2026-07-11 - 修复弹窗透明和滚动

- 修改文件：Settings.tsx, DocumentUpload.tsx, index.css
- 两大问题：弹窗透明看不清（overlay bg-black/10 太淡）；无法滚动（overflow-hidden 切断滚动条）
- 修复：index.css 全局覆盖 `[data-slot=dialog-overlay]` 为 `rgba(0,0,0,0.65)` + blur；DialogContent 显式 `bg-surface-900` 实体背景；移除 overflow-hidden

## 2026-07-11 - 修复滑块、滚动条、新建按钮

- 修改文件：components/ui/slider.tsx, components/ui/scroll-area.tsx, Settings.tsx, DocumentUpload.tsx
- 三大问题：
  1. 滑块只有 thumb 没有 track：原 Slider 用 `bg-muted` (背景同色) + thumb `bg-white size-3` (太小)，暗色下完全不可见
  2. 右侧编辑器无法滚动：base-ui ScrollArea 必须用 `<ScrollAreaContent>` 包裹内容才能正确撑开高度，原 shadcn 模板缺这个组件
  3. 「新建 Agent」按钮看不见：variant="outline" 在暗色下几乎隐形
- 修复：
  - Slider：track `bg-white/15 h-1.5`；thumb `size-4 border-2 border-primary bg-white` + 青色辉光 + hover 放大
  - ScrollArea：补上 `<ScrollAreaPrimitive.Content>` 包装；显式 `overflowY: auto`；thumb `bg-white/30 hover:bg-white/50`
  - New Agent 按钮：`variant="secondary"`
- 构建通过，CSS 78KB / JS 530KB

## 2026-07-11 - 重写颜色主题提高对比度

- 修改文件：index.css, ChatInterface.tsx
- 关键改动：把所有 shadcn 主题变量从 oklch 改为 hex 值，大幅提高对比度
  - --background: #0b0b16 (纯黑底)
  - --foreground: #e6e8ee (浅灰白文字)
  - --card: #14141f (对比明显)
  - --primary: #00e5ff (亮青色，原 oklch 0.68 太暗)
  - --muted-foreground: #9098a8 (原 oklch 0.55 太灰看不清)
  - --accent: #a78bff (亮紫色)
  - --destructive: #ff4d6d (亮红)
  - --border: rgba(255,255,255,0.12) (原 0.06 几乎不可见)
  - --input: rgba(255,255,255,0.10)
- cyber 颜色提亮：cyber-500 从 #00bcd4 → #00e5ff；neon-400 从 #7c4dff → #a78bff
- surface 调色板微调，色阶间距更明显
- glass-panel 透明度从 0.06 → 0.10，darken 一点
- ChatInterface 背景从 cyber-gradient 改为纯色 #0b0b16，避免渐变模糊
- 构建通过，CSS 78KB / JS 530KB

## 2026-07-11 - 移除 Sidebar Agent 下拉中的"管理 Agent"入口

- 修改文件：frontend/src/components/Sidebar.tsx
- 删除 Agent 下拉菜单底部的"管理 Agent"按钮
- 原因：与侧边栏底部"Agent 设置"入口重复，违反单一入口原则
- 保留底部 "知识库管理" 和 "Agent 设置" 两个统一入口
- 构建通过

## 2026-07-11 - 集成 Mem0 长期记忆

- 安装 mem0ai (v2.0.11) + qdrant-client/pydantic v2/protobuf 等
- 新建 backend/app/core/memory.py 记忆服务模块
- 配置 DashScope LLM + Embedding，Qdrant 内嵌模式
- chat.py: 第6步搜索记忆注入 system_prompt; finally 保存对话到 Mem0
- 所有异常静默处理，不阻塞聊天

## 2026-07-11 - 修复 Textarea forwardRef 警告

- 修改文件：frontend/src/components/ui/textarea.tsx
- 问题：MessageInput 传 ref={textareaRef} 给 Textarea，运行时警告"Function components cannot be given refs"
- 原因：shadcn 初始的 Textarea 是普通函数组件，没有 forwardRef 包装，无法接收 ref
- 修复：用 React.forwardRef 包装 Textarea，类型为 HTMLTextAreaElement
- 验证：HMR 自动刷新，警告消失

## 2026-07-11 - 修复 Mem0 跨会话记忆 — user_id 从 conv_id 改为 IP

- 修改文件：backend/app/api/chat.py
- 问题：Mem0 记忆保存用 user_id=conversation_id（UUID），每次新对话 ID 不同，跨对话检索不到
- 修复：chat endpoint 提取客户端 IP (X-Real-IP > X-Forwarded-For > req.client.host)
- 把 IP 作为 user_id 传给 _prepare_chat 和 _stream_response
- search_memories 和 add_memories 都用 IP 作为 user_id
- 同一 IP 下所有对话共享记忆，换会话也能查到
- Python 语法检查通过

## 2026-07-11 - 增加上下文窗口用量显示

- 修改文件：ChatInterface.tsx, Sidebar.tsx
- **聊天区顶部进度条**（有消息时显示）：
  - 估算 tokens（总字符数 / 2，混合中英文实用估算）
  - 1px 细进度条：支持 cyan → amber(>65%) → red(>85%) 三色切换
  - 右侧显示 "1,234 / 4,096 30%" 格式
- **侧边栏对话列表**：每个对话行尾显示 message_count
- 使用 useMemo 缓存计算，消息变化不触发额外 render
- 构建通过

## 2026-07-11 - 消息列表自动滚动到最新位置

- 修改文件：frontend/src/components/MessageList.tsx
- 新增 useRef + useEffect 实现 auto-scroll
- 消息列表末尾添加不可见锚点 <div ref={bottomRef} />
- useEffect 监听 messages 变化 → scrollIntoView
- 流式 token 推送时用 auto（即时），进入会话时用 instant（无动画跳转）
- 构建通过

## 2026-07-11 - 增加会话上下文自动压缩功能

- 修改文件：backend/app/api/chat.py
- 新增 `_estimate_tokens()` — 字符数/2 估算 token
- 新增 `_compress_context()` — 当消息超过 max_tokens × 70% 时触发压缩
  - 保留最近 6 条消息不动
  - 用 LLM (qwen-plus, temp=0.3) 把更早的消息压缩为 200 字中文摘要
  - 摘要存入 DB（删除旧消息 → 插入 system role 压缩消息）
  - LLM 调用失败时回退到截断文本
  - 已压缩过的对话跳过（检测 system role + "压缩" 标记）
- `_messages_to_langchain` 新增 system role 处理
- `_prepare_chat` 第 5.5 步：加载消息后调 _compress_context
- Python 语法检查通过

## 2026-07-11 - API 接口全面改造为企业级 POST + 安全设计

- **目标**：将所有 GET 接口改为 POST（防止敏感数据暴露在 URL 中），按企业级标准添加安全层
- **后端改动：**
  - `app/schemas.py`：新增 PaginationRequest、IdRequest（含路径遍历/注入字符过滤）、ConversationListRequest、ConversationGetRequest、ConversationDeleteRequest、ConversationTitleUpdate、RagDocumentListRequest、RagDocumentGetRequest、RagDocumentDeleteRequest、AgentListRequest、AgentGetRequest、AgentDeleteRequest。所有请求模型添加 max_length/min_length 约束
  - `app/main.py`：
    - API 版本化：所有路由添加 `/api/v1` 前缀
    - 新增 `security_headers_middleware`：注入 X-Request-ID、X-Content-Type-Options、X-Frame-Options、X-XSS-Protection、Referrer-Policy、Permissions-Policy、Cache-Control 等安全头
    - 新增 `request_size_guard`：POST 请求体 10MB 硬上限（413）
    - CORS 收紧：allow_methods 仅 POST + OPTIONS；allow_headers 白名单
    - 生产环境关闭 ReDoc，Swagger 移至 /api/docs
    - 慢请求审计日志（>3s）
  - `app/api/health.py`：GET → POST `/api/v1/health`
  - `app/api/rag.py`：GET → POST（/documents/list, /documents/get, /documents/delete, /stats）；上传路径 `/upload` → `/documents/upload`
  - `app/api/conversations.py`：GET → POST（/list, /get, /delete）；PATCH → POST（/update-title，title 从 query string 移到 body）
  - `app/api/agents.py`：GET → POST（/tools, /list, /get, /delete）；PUT → POST（/update，agent_id 移到 body）
  - `app/deps.py`：新增 get_client_ip（含 header 注入防护）、通用 verify_rate_limit、分层读写限流（write: 10/60s, read: 60/60s）
- **前端改动：**
  - `api/client.ts`：API_BASE 改为 `/api/v1`；fetchJSON 统一 POST；所有函数适配新路径和签名
- 验证：Python schemas 全部通过（路径遍历拦截、字段约束生效）、所有 .py 语法检查通过、TypeScript tsc --noEmit 零错误

## 2026-07-11 - 为 RAG 添加 BM25 混合检索（向量 + 关键词）

- **目标**：原先只有 pgvector 纯语义检索，缺少关键词精确匹配能力（专有名词、代码等），补齐 BM25 后形成企业级混合检索
- **实现方案**：
  - PostgreSQL tsvector + GIN 索引做关键词全文检索
  - pgvector cosine_distance 做语义检索
  - RRF (Reciprocal Rank Fusion, k=60) 融合两路结果
- **CJK 中文分词**：PG 的 `simple` config 不分割中文，需要预处理
  - Python 侧 `_cjk_tokenize()`：正则 `[\u4e00-\u9fff]` 在每格 CJK 字符前后插空格，让 tsvector 逐字索引
  - 避免 PL/pgSQL 中的 `\u` 转义兼容问题，统一在 Python 侧分词后写入
- **改动文件：**
  - `app/database.py`：`init_db()` 新增 tsvector 列（`ALTER TABLE ADD IF NOT EXISTS`）、GIN 索引、`_reindex_existing_chunks()` 启动时重建已有数据
  - `app/rag/retriever.py`：新增 `_cjk_tokenize()`、`keyword_search()`、`vector_search()`、`_rrf_fuse()`、`hybrid_search()`；`index_document()` 索引时同步填充 search_vector
  - `app/api/rag.py`：搜索端点改用 `hybrid_search` 替代 `search_similar`
  - `app/agent/nodes.py`：RAG 节点改用 `hybrid_search`
- **测试验证**：
  - `bigram_tsvector` 函数 → 正确定位：regexp `[一-龥]` → 已验证 `'([一-龥])' = '([\\u4e00-\\u9fff])'`，需用 E-string 写法
  - BM25 "西瓜" 关键词匹配 → 1 结果 ✓
  - "电瓶车" → 0.5107 RRF（关键词+语义）> "人工智能" → 0.3938（纯语义）
  - "哥们儿 西瓜 大棚" 多关键词 → 0.8940 最高分 ✓

## 2026-07-11 - 每条对话消息增加复制功能

- 修改文件：frontend/src/components/MessageList.tsx
- 新增 Copy/Check 图标（lucide-react），每条消息气泡右上角添加复制按钮
- 交互：
  - 默认隐藏，hover 消息气泡时淡入显示
  - 点击复制 → 图标切换为 Check + 绿色背景，2 秒后恢复
  - `navigator.clipboard.writeText()` 为主，旧浏览器 fallback 到 textarea + execCommand
- 用户消息和 AI 消息均有独立的 hover 配色
- tsc --noEmit 零错误

## 2026-07-11 - 调整复制按钮位置（用户反馈）

- 反馈：复制按钮放在消息气泡内右上角不好
- 修改文件：frontend/src/components/MessageList.tsx
- 改为：消息气泡**外部**、右下角的 meta row（时间戳 + 复制按钮并排），参考用户截图
- 用户消息方向反转（flex-row-reverse），保持视觉一致
- 图标缩小到 11px，更精致；始终可见（不需要 hover），时间戳是 `HH:MM` 格式
- 复制成功时图标变绿色 Check，2 秒后恢复
- tsc 零错误

## 2026-07-11 - 刷新保持当前选中会话（URL hash 持久化）

- 问题：刷新页面后 conversationId（React state）丢失，回到空白页
- 修复：App.tsx 中用 `window.location.hash` 做轻量 URL 状态管理
  - `useEffect([conversationId])`：选中/创建会话时 → `window.location.hash = conversationId`
  - `useEffect([])`（mount）：页面加载时读取 `hash` → `loadConversation(hash)` 恢复
  - 新建对话时 `history.replaceState` 清除 hash
- 好处：刷新不丢状态、可书签、可分享链接
- tsc 零错误

## 2026-07-11 - AI Agent 管理后台（antd）+ 大模型选择器

- **后端改动：**
  - `app/schemas.py`：ChatRequest 新增 `model_provider` 字段；新增 DashboardStats、LLMProviderInfo、LLMConfigResponse schema
  - `app/api/admin.py`：新建管理后台 API 路由
    - `POST /admin/dashboard` — 聚合统计（对话/消息/文档/分块/存储/Agent）
    - `POST /admin/llm/list` — 列出 3 个 LLM 提供商及其状态
    - `POST /admin/models` — 前台公开模型列表
  - `app/api/chat.py`：_prepare_chat 从 request.model_provider 传 provider 给 agent_config
  - `app/main.py`：注册 admin router
- **前端改动：**
  - 安装 antd + @ant-design/icons
  - `components/AdminPage.tsx`：全新管理后台（antd Layout + Menu + Card + Table + Statistics）
    - 仪表盘：6 个 Statistic 卡片 + LLM 提供商状态卡片
    - 知识库管理：文件表格
    - 大模型配置：3 个提供商详情卡片
  - 改为纯 CSS-in-JS 暗色科技风（无 antd 组件，匹配主应用 cyberpunk 设计语言）
  - 仪表盘：4 个渐变卡片 + 使用率进度条 + LLM 提供商状态卡片（顶部渐变线）
  - 知识库：自定义表格行（hover 高亮 + 状态圆点 + 文件类型标签）
  - 大模型：3 个提供商详情卡片（配置项网格 + 模型标签云）

## 2026-07-11 - 大模型选择器移到输入框右侧

- 反馈：原来 antd Select 在输入框上方居中太丑
- 修改文件：MessageInput.tsx（自建 ModelSelector 组件），ChatInterface.tsx（移除 antd 引用和顶部工具栏）
- 新设计：紧凑 icon 按钮放在 RAG 按钮左侧
  - 显示当前模型名称 + 状态圆点（青色高亮表示已选择）
  - 点击展开：上方弹出下拉面板，背景 `bg-[#0b0b16]/95 backdrop-blur-xl`
  - 列表项：名称 + 默认模型小字 + 选中状态 Check
  - 底部"管理模型"快捷入口跳后台
- 卸载 antd 依赖（已不再使用）
- tsc + vite build 通过

## 2026-07-11 - 管理知识库 + 受保护 RAG 专用 Agent

- **后端改动：**
  - `app/models.py`：Document 新增 `source` 字段 (user/admin)；AgentConfig 新增 `is_protected` 字段
  - `app/database.py`：自动迁移 SQL（ADD COLUMN IF NOT EXISTS source / is_protected）
  - `app/main.py`：`_seed_default_agent()` 创建受保护的「知识库助手」Agent（id="rag-assistant"，仅启用 rag，is_protected=True）
  - `app/rag/retriever.py`：vector_search / keyword_search / hybrid_search 均新增 `source` 参数，按文档来源过滤
  - `app/api/agents.py`：delete 端点新增 is_protected 检查（受保护 Agent 无法删除）
  - `app/api/admin.py`：新增管理端 RAG CRUD（POST rag/documents / rag/upload / rag/delete / rag/stats），上传时强制 source="admin"
- **前端改动：**
  - `api/client.ts`：新增 adminRagDocuments / adminRagUpload / adminRagDelete / adminRagStats
  - `AdminPage.tsx`：知识库管理页重写 — 标题改为"管理员知识库文档"，新增「+ 上传文档」按钮、每行「删除」操作；加载数据切换到 admin API
- **行为区分：**
  - 前台用户上传文档 → source="user" → 普通 Agent 只查用户文档
  - 管理后台上传文档 → source="admin" → 「知识库助手」Agent 只查管理员文档
  - 「知识库助手」Agent 受保护，API/UI 均无法删除
- 验证：后端迁移成功、受保护 Agent 创建成功、删除被 400 拦截、前端 Vite 构建通过
  - `components/ChatInterface.tsx`：顶部工具栏新增模型选择器（antd Select）+ 管理后台入口
  - `hooks/useChat.ts`：sendMessage 接收 modelProvider 参数
  - `api/client.ts`：streamChat 新增 modelProvider；新增 adminDashboard/adminLlmList/adminModels
  - `App.tsx`：新增 modelProvider 状态、loadModels、admin 入口渲染
  - `index.css`：antd 暗色主题覆盖
- tsc 零错误，后端 admin API 验证通过
